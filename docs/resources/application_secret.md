---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "skycloak_application_secret Resource - skycloak"
subcategory: ""
description: |-
  Rotates an application's client secret. The secret is regenerated whenever this resource is created or rotate_when changes.
---

# skycloak_application_secret (Resource)

Rotates an application's client secret. The secret is regenerated whenever this resource is created or `rotate_when` changes.

## Example Usage

```terraform
resource "skycloak_application_secret" "web" {
  cluster_id = skycloak_cluster.production.id
  realm_name = skycloak_realm.app.name
  client_id  = skycloak_application.web.client_id

  # Change any value here to force a new client secret to be generated.
  rotate_when = {
    rotated_on = "2026-01-01"
  }
}

output "web_rotated_secret" {
  value     = skycloak_application_secret.web.client_secret
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `client_id` (String) Application client ID. Immutable.
- `cluster_id` (String) Cluster ID. Immutable.
- `realm_name` (String) Realm name. Immutable.

### Optional

- `rotate_when` (Map of String) Arbitrary key/value map; changing any value forces a new secret to be generated.

### Read-Only

- `client_secret` (String, Sensitive) The current (rotated) client secret.
- `id` (String) Composite ID `cluster_id/realm_name/client_id`.
